vcluster:
  controlPlane:
    distro:
      k3s:
        enabled: true
    proxy:
      extraSANs:
        - "preview-env.team-alpha.workloads.example.com"
        - "192.168.44.200"
        - "192.168.44.201"
    statefulSet:
      persistence:
        volumeClaim:
          enabled: true
          size: 5Gi

  # Schema fix: networking.advanced doesn't take 'enabled'
  # Just use the object if you need to define specific CIDRs
networking:
  serviceCIDR: "10.96.0.0/12"

# Schema fix: sync uses direct mapping now
sync:
  ingresses:
    enabled: true

# Custom variables (Top Level)
teamName: "team-alpha"
appName: "preview-env"
ingress:
  enabled: true